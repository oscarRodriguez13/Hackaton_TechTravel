<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Color de fondo del rectángulo -->
    <solid android:color="#FFFFFF"/>
    <!-- Configuración de los bordes redondeados -->
    <corners
        android:bottomRightRadius="10dp"
        android:bottomLeftRadius="10dp"
        android:topLeftRadius="10dp"
        android:topRightRadius="10dp"/>
    <!-- Añadir un borde alrededor del rectángulo -->
    <stroke
        android:width="1dp"
        android:color="#B6B6B6"/> <!-- Puedes cambiar el color y el grosor del borde aquí -->
    <!-- Opcional: padding dentro del rectángulo -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
</shape>
